package model

import "easyserver/internal/envconfig"

// EnvConfig is now defined in easyserver/internal/envconfig.
// Kept as alias for backward compatibility.
type EnvConfig = envconfig.EnvConfig

// PathEntry is now defined in easyserver/internal/envconfig.
// Kept as alias for backward compatibility.
type PathEntry = envconfig.PathEntry

// GlobalConfig is now defined in easyserver/internal/envconfig.
// Kept as alias for backward compatibility.
type GlobalConfig = envconfig.GlobalConfig
